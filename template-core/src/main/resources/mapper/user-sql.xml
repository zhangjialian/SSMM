<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjl.dao.UserDao">

	<select id="getUserList" parameterType="java.util.Map" resultType="com.zjl.common.user.UserDO">
		select id, name, username, email
			from user
	</select>

	<insert id="insertUser" parameterType="com.zjl.common.user.UserBO">
		insert into user(name, username, email)
			values(#{name}, #{username}, #{email})
	</insert>

	<select id="getUserById" parameterType="int" resultType="com.zjl.common.user.UserBO">
			select id, name, username, email
			from user
			where id = #{id}
			limit 1
	</select>

	<update id="updateUserById" parameterType="com.zjl.common.user.UserBO">
		update `user`
		set name = #{name}, username = #{username}, email = #{email}
		where id = #{id}
	</update>

</mapper>